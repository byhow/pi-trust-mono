# pi-agent-permit

## 0.1.0

- Add a fail-closed `tool_call` policy hook for Pi and OMP.
- Evaluate exact tool input through the external sisyphus CLI with bounded stdin/stdout and sanitized process state.
- Add non-executing diagnostic tool and command surfaces.
