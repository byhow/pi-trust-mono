# pi-mcp-vet

## 0.1.0

- Add a versioned `mcp-connect` decision contract.
- Vet HTTP and stdio transport, package provenance, roots, capabilities, and credential exposure without executing the server.
- Add a Node CLI plus matching Pi/OMP extension tool and command.
