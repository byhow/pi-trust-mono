# pi-model-choice

## 0.1.0

- Add a validated process adapter for `model-picker.selection` v1.
- Add advisory `model_choice` and `/model-choice` Pi/OMP surfaces.
- Keep model ranking and model switching outside this package.
