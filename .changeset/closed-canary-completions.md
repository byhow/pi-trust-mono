---
"pi-sisyphus": patch
---

Add package-private, bounded native tool-result correlation and immutable metadata-only completion receipts for allowed Fleet Lab canaries without changing the public trust-evaluation interface.
